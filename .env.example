# Copy to .env for local docker compose (optional):
#   cp .env.example .env
#
# Coolify: set these in the service Environment / Secrets UI instead.
# Do not wrap values in quotes.

# --- Auth (pick one) ---
# A) Env vars (works with plain docker compose / Coolify):
DEEPSEEK_TOKEN=
DEEPSEEK_HIF_DLIQ=
DEEPSEEK_HIF_LEIM=
DEEPSEEK_COOKIE=
DEEPSEEK_WASM_URL=https://fe-static.deepseek.com/chat/static/sha3_wasm_bg.7b9ca65ddd.wasm

# B) Auth file: copy deepseek-auth.json to ./config/ and run with storage compose:
#   docker compose -f docker-compose.yml -f docker-compose.storage.yml up -d
